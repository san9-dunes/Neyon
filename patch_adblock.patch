--- app/src/main/kotlin/io/github/landwarderer/futon/core/network/webview/adblock/AdBlock.kt
+++ app/src/main/kotlin/io/github/landwarderer/futon/core/network/webview/adblock/AdBlock.kt
@@ -52,6 +52,15 @@
		} ?: true
	}

+	@WorkerThread
+	fun getMatchingCssRules(url: HttpUrl?): String? {
+		if (!settings.isAdBlockEnabled) {
+			return null
+		}
+		return synchronized(this) {
+			rules ?: parseRules().also { rules = it }
+		}?.getCssRules(url)
+	}
+
	@WorkerThread
	private fun parseRules() = runCatchingCancellable {
